spring:
  profiles:
    active: mysql
---
spring:
  profiles: mysql
  datasource:
    url: jdbc:mysql://localhost:3306/finut?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: finutflab
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql

